# This is a Databricks asset bundle definition for FHIR_API_Data_Ingestion_and_Analytics_Assignment.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: FHIR_API_Data_Ingestion_and_Analytics_Assignment
  uuid: 24079c8f-8f3f-4120-bb6d-9ded8a798946

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c6c23bfe-7dea.cloud.databricks.com
    variables:
      catalog: fhir_api_data_ingestion_and_analytics_assignment
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-c6c23bfe-7dea.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/rahulgaur0304@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rahulgaur0304@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: fhir_api_data_ingestion_and_analytics_assignment
      schema: prod
    permissions:
      - user_name: rahulgaur0304@gmail.com
        level: CAN_MANAGE
